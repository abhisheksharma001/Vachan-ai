# Vachan.ai frontend environment template.
# Copy this file to `.env.local` and adjust as needed.
# This variable is read server-side by the Next.js BFF route handlers.

# URL of the FastAPI backend.
# Use 127.0.0.1 (not localhost) if your backend binds to IPv4.
BACKEND_URL=http://127.0.0.1:8000
